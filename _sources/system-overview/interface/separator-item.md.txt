# BSeparatorItem

A {hclass}`BSeparatorItem` is used to visually distinguish between groups
of {cpp:class}`BMenuItem`s in a {cpp:class}`BMenu`.

The {hclass}`BSeparatorItem` is shown as a horizontal rule within the menu
which cannot be selected and has no associated functionality.
